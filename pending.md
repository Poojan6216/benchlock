# Pending work — handoff for a new chat

Written 2026-09-05 at 23:50, just before a full shutdown. Two background jobs were still
running on this machine and **died with the shutdown**; everything else is committed.
A fresh session should be able to finish from this file alone. Delete this file in the
final commit.

## Where things stand

- The post-build gap audit found 14 confirmed gaps; **all 14 are fixed and committed** in
  five commits on top of `ce29073` (see `git log --oneline | head -6`):
  `fix(stats,attribute)`, `fix(judge)`, `fix(bench)`, `ci`, `docs`.
- The README now has an "In plain language" section, generated by
  `scripts/gen_readme.py` from `bench/results/` (no hand edits — `README.md` is generated).
- Fast test suite: 452/452 passed on the final engine. ruff, ruff format, mypy --strict clean.
- Headline sim, adversarial suite, Tier 2 (real-judge) analysis, demos and dogfood were all
  re-run against the final engine and are committed.
- **Uncommitted, keep it:** `BUILD_SPEC.md` — new Progress Log lines `[6.1, 6.2, 6.3, 6.4,
  6.5]` and `[AUDIT]`, plus Final Report edits (headline numbers 21.2% / 7.4%, audit section,
  decisions 12–14). Three lines in it still carry numbers from the OLD grid and must be
  updated after the new grid (see step 3).
- `bench/results/noise-floor-earns-its-place.json` may show as modified: the slow test
  `tests/test_noisefloor.py` rewrites it. If `git status` shows it, check it is valid JSON
  (`python3 -m json.tool bench/results/noise-floor-earns-its-place.json > /dev/null`); if
  the shutdown truncated it, `git checkout -- bench/results/noise-floor-earns-its-place.json`.

## The two jobs that were killed — re-run both

Because `decide()` changed in the audit fixes, **every simulation number must be
regenerated against the final engine (Hard Rule 5)**. The headline cells are done; the
full grid is not.

1. **Full simulation grid** (single-threaded, ~45–55 min, no API key, no cost):

       uv run python bench/sim/run_sim.py --all --seeds 100 --horizon 60

   Writes `bench/results/sim-latest.json` only at the very end (it was at 91/192 cells when
   the machine went down; nothing partial is kept). Run it in the background and do other
   work meanwhile.

2. **Slow test suite** (9 tests, ~10–15 min, can run concurrently with the grid):

       uv run pytest -q -m slow --durations=9

   Expected: 9 passed. It rewrites `noise-floor-earns-its-place.json` (see above).

## After the grid finishes — in this order

3. Regenerate every generated document, in dependency order:

       uv run python scripts/gen_results.py        # RESULTS.md + bench/results/summary.json
       uv run python scripts/gen_guarantees.py     # docs/statistical-guarantees.md
       uv run python scripts/gen_threat_model.py   # docs/threat-model.md
       uv run python scripts/gen_writeup.py        # docs/writeup.md (reads summary.json)
       uv run python scripts/gen_readme.py         # README.md (reads summary.json, docs/demo.md)
       uv run python bench/plots/make_plots.py     # docs/plots/*.png from sim-latest.json

   Then update the three Final Report lines in `BUILD_SPEC.md` that quote the old grid
   (search for `roughly 8x`, `**3.1 runs**`, `**24.6 runs**`, `8x is the honest price`)
   with the new values from `bench/results/summary.json` →
   `aggregates["antiresult.b1_mean_delay"]`, `["antiresult.b6_mean_delay"]`,
   `["antiresult.delay_ratio"]` (render delays with one decimal, the ratio as `Nx`).
   Do not rewrite older Progress Log entries; append one new line instead, e.g.
   `[5.3, 5.4, 5.6, 7.10, AUDIT] Grid regenerated against the final engine: ...` quoting the
   new B1/B6 delays, the B1 vs B6 grid false-alarm rates and B6's judge->system
   misattribution and indeterminate rates (old values for comparison: B1 false alarm 0.234,
   B6 0.000, B6 judge->system 0.006, B6 indeterminate 0.104, B1 delay 3.1, B6 delay 24.6).
   Use `uv run python scripts/logprogress.py "<line>"` to append it.

4. Verify, all must pass:

       uv run python scripts/check_numbers.py      # every README/RESULTS number traces to bench/results/
       uv run python scripts/check_done.py         # Definition of Done, 14/14
       uv run ruff check . && uv run ruff format --check . && uv run mypy
       uv run pytest -q -m "not slow"              # 452 passed (slow suite done in step 2)

5. Commit (one commit for results+docs+plots, one for the spec — or one combined), then
   confirm the generated-docs gate on the clean tree:

       git add bench/results/sim-latest.json bench/results/summary.json bench/results/noise-floor-earns-its-place.json \
               README.md RESULTS.md docs/*.md docs/plots/ BUILD_SPEC.md
       git rm --cached pending.md 2>/dev/null; rm pending.md
       git commit   # message: "bench(results): regenerate the grid, documents and plots against the final engine"
       uv run python scripts/check_generated.py    # must print "ok: 7 generated files match their generators"

6. Report back to Poojan: the regenerated grid numbers (delay ratio, false alarms,
   misattribution), that all checks pass, and the commit hash. The audit findings and the
   plain-language explanation were already delivered in chat on 2026-09-05.

## Standing rules (do not relax)

- Never print or paste the API key; it lives only in `.env` (gitignored). Nothing in the
  steps above needs it — no API calls, no cost.
- Never commit `data/`.
- No `git push` (no remote) and **no PyPI publish** without Poojan's explicit go-ahead.
- Commits: Conventional Commits; author is the repo-local config `Poojan Patel
  <poojanpatel119@gmail.com>`; end every message with
  `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.
- Never invent a number: every figure in a document must come from `bench/results/`.
